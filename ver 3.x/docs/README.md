Вентилятор на ИК пульте ver3.0

  Напольный вентилятор с управлением через ИК пульт (ТОЛЬКО по протоколу NEC).

 Переделанная версия из 2.1, переработал код, исправил кучу косяков.
  Схема и микроконтроллеры остались те же — ATmega48 и ATmega8, убрал лишние библиотеки, оставил только обновлённую NecDecoder с зависимостью GyverIO.
  

  ОПИСАНИЕ:
  Вентилятор имеет 4 режима скорости: первые три скорости штатные, четвёртый с пониженными оборотами (через гасящий конденсатор).
  Возможность управлять поворотами вентилятора через Servo мотор:
    - Ручные повороты влево-вправо с помощью нажатия и удержания соответствующей кнопки с пульта;
    - Автоповороты влево-вправо кратковременным нажатием кнопки с пульта и отключением другой.

  Любая из четырёх скоростей включается с таймером, по истечению которого вентилятор выключится и выключит автоповороты (если они были включены).
  Время таймеров зашито в прошивку и легко меняется на вкладке "config.h", так же на той же вкладке меняются все остальные настройки.

  Всего имеется 2 таймера для всех 4-х режимов.
  При включении питания положение Servo сохраняется в том же состоянии, в котором оно было остановлено перед отключением.
  Положение серво сохраняется в EEPROM при:
    - Нажатии кнопки Off (Выключить ВСЁ);
    - Нажатии кнопки ↓ Авто (откл. автоповоротов);
    - Отключение любого из двух таймеров;
    - После нажатия, удержания и отпускания кнопок < Влево или Вправо > (ручные повороты Servo).

  P.S. При ручных поворотах, что бы не "насиловать" EEPROM на запись при долгом удержании и отпускании кнопок "< Влево или Вправо >" используется отложенное сохранение (через 7 сек.), после чего происходит сохранение положения Servo.

  Для замены кодов кнопок на свои использовать скетч "IR_Decoder" + любую Arduino с TTL конвертером (что бы выводить 8-ми битные коды кнопок в Serial).

  Перед компиляцией выбрать нужный МК: ATmega48 или ATmega8.
  И на вкладке "config.h" изменить параметры WatchDog под выбранный МК, при необходимости изменить другие настройки и загрузить код.


  Параметры платы в Arduino IDE (MiniCore v. 3.1.2):
  1) Плата: MiniCore -> "ATmega48" или "ATmega8"
  2) BOD: BOD 4.3V (для ATmega8 4.0V)
  3) Bootloader: No bootloader
  4) Clock: External 16MHz
  5) EEPROM: EEPROM retained
  6) Compiler LTO: LTP enabled
  7) Variant: 48/48A (для ATmega48)
  ----------------------------
  8) Программатор: USBasp
  После выставления параметров нажать: Записать Загрузчик.



  ИЗМЕНЕНИЯ В ВЕРСИЯХ КОДА:

  Версия: 2.0 [начальный релиз] (16.03.2023).

  Версия: 2.1 (30.04.2023):
  - Исправил ошибку в названии кнопки OFF (была написана как OK), что бы не вводила в заблуждение;
  - Добавил возможность Вкл./Выкл. LED индикаторы режимов с пульта одной кнопкой (что бы ночью не мешали);
  - Заменил в некоторых местах "digitalWrite()" на управление конкретными пинами LED через резистры портов для уменьшения размера кода (что бы уместить код в ATmega48).
  
    Версия: 3.0 (17.06.2026):
  - Переписал код старой версии 2.1, оптимизировав его, удалив лишние библиотеки и обновил библиотеку NecDecoder с зависимостью GyverIO.


  Автор: Otto
  Лицензия: Лицензия: MIT (см. LICENSE)
  Версия изменения: 3.0
  Создан: 17.06.2026
